#pragma once

#include "src/geometry/types.hpp"

// TODO(jyan): poc에서는 일단 유틸만. 추후에 필요하면 다른 함수 추가
namespace folio::collision
{
using folio::geometry::AABB;
using folio::geometry::circleAabb;
using folio::geometry::Transform;
} // namespace folio::collision
